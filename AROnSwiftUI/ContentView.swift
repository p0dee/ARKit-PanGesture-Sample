import ARKit
import SwiftUI

struct ContentView: View {
    var body: some View {
        ARView()
            .padding()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
